```ts
import jwt from "josnwebtoken";

jest.mock("jsonwebtoken", () => ({
  ...jest.requireActual<typeof jwt>("jsonwebtoken"),
  decode: jest.fn(),
  verify: jest.fn(),
}));

const { verify, decode } = jwt as jest.Mocked<typeof import("jsonwebtoken")>;

beforeEach(() => {
  verify.mockReset();
  decode.mockReset();
});

it("", () => {
  verify.mockImplementation(() => {
    throw new jwt.TokenExpiredError("Token Expired", new Date());
  });
});
```
