```ts
const mockFetchQuotes = jest.fn();
jest.mock("../../../services/quotes", () => ({
  __esModule: true,
  default: () => mockFetchQuotes(),
}));

it("", () => {
  mockFetchQuotes.mockImplementation(() => Promise.resolve({ status: 200, data: { quotes: [] } }));
});
```

```ts
jest.moc("../../services/quotes", () => {
  return jest.fn().mockImplentation(() => Promise.resolve({ status: 200, data: {} }));
});
```
