```ts
jest.mock("../src/utils", () => ({
  createUUID: jest.fn(() => 12345),
}));
```

```ts
jest.mock("@mrv/mrv-utils", () => ({
  createUUID: jest.fn(() => 12345),
  maskEmail: jest.fn((email, mask) => (mask ? "j****@example.com" : email)),
}));
```

## mocking whole utils

```ts
import * as utils from '../../utils/utils'

jest.mock('../../utils/utils')
const mockedUtils= utils as jest.Mocked(typeof utils)


mockedUtils.getUserInfo.mockImplementation(()=>({SalesID:''}))

```
