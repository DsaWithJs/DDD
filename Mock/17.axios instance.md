```ts
import https from "https";
import axios from "axios";

const httpsAgent = new https.Agent({
  rejectUnauthorized: false,
});
const axiosInstance = axios.create({ httsAgent });

export { axioInstance };
```

```ts
import { axioInstance } from "@mrv/axios";
jest.mock("@mrv/axios");

const mockedAxioInstance = axiosInstance as jest.Mocked<typeof axiosInstance>;

it("", () => {
  mockedAxioInstance.get.mockImplementation(() => {
    return Promise.resolve({
      statusCode: 200,
      satus: 200,
      data: {},
    });
  });
});
```

```ts
import axios from "axios";

const mockAxiosError = jest.fn().mockImplementation(() => false);
jest.mock("axios", () => ({
  ...jest.requireAction<typeof axios>("axios"),
  isAxiosError: () => mockAxiosError(),
}));
```

```ts
import axios from "axios";
jest.mock("axios", () => ({
  ...jest.requireAction<typeof axios>("axios"),
  isAxiosError: () => {
    return false;
  },
}));
```
