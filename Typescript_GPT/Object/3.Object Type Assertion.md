```typescript
interface FormData {
  startDate: Date;
  endDate: Date;
  storeLocation: string;
  searchAllLocations: boolean;
  salesId: string;
  jobId: string;
}

const formErrors: { [key: string]: string } = {
  startDate: "Invalid start date",
  endDate: "Invalid end date",
  storeLocation: "Location required",
  searchAllLocations: "Must be a boolean value",
  salesId: "Invalid sales ID",
  jobId: "Invalid job ID",
} as { [P in keyof FormData]: string };
```
